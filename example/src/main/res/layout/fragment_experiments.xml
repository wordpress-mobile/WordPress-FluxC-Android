<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Platform to query"
        tools:ignore="HardcodedText" />

    <Spinner
        android:id="@+id/platform_spinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Widget.AppCompat.Spinner.Underlined"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Anonymous ID for this participant"
        tools:ignore="HardcodedText" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <EditText
            android:id="@+id/anon_id_edit_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Anonymous ID"
            android:inputType="text"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/generate_anon_id_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Generate"
            tools:ignore="HardcodedText" />
    </LinearLayout>

    <Button
        android:id="@+id/fetch_assignments"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Fetch assignments"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/get_cached_assignments"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Get cached assignments"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/clear_cached_assignments"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Clear cached assignments"
        tools:ignore="HardcodedText" />

</LinearLayout>
