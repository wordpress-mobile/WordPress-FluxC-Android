apply plugin: 'java'
dependencies {
    testImplementation sharedLibs.restAssured
    testImplementation sharedLibs.junit
    testImplementation sharedLibs.json
}

test {
  testLogging {
    events "passed", "skipped", "failed", "standardOut", "standardError"
  }
}

repositories {
    google()
}
